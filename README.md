# Aangan Digital Menu

Warm, simple, mobile-first React/Vite digital menu for QR-code visitors.

## Run

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
npm run preview
```

The seven menu pages are stored locally in `public/assets/`, so the site does not depend on Kommodo embeds.
